name: mi_tienda
description: Sistema POS completo con Flutter
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  mobile_scanner: ^5.1.1
  image_picker: ^1.1.2
  intl: ^0.19.0
  share_plus: ^9.0.0
  shared_preferences: ^2.2.3
  font_awesome_flutter: 11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true